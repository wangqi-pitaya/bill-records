# 需求说明文档

> **版本**: v0.1.0  
> **项目名称**: 简易记账本  
> **更新日期**: 2026-07-03

---

## 1. 项目概述

一款面向个人用户的轻量级记账应用，支持记录日常收入与支出，提供分类管理、账单统计、按时间筛选等功能。数据存储在本地，无需后端服务，适合快速记录和查看个人财务状况。

---

## 2. 功能需求

### 2.1 首页 - 账单列表

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| R-001 | 页面顶部固定标题栏，显示当前选中年月，点击弹出日期选择弹层 | 高 |
| R-002 | 日期选择弹层支持按年/按月两种模式切换，按年只选年份，按月选年份+月份 | 高 |
| R-003 | 当所选年份无账单数据时，自动向后跳转到有数据的年份；若无任何数据则停留在当前年份 | 中 |
| R-004 | 显示统计卡片，汇总当前筛选范围内的收入、支出和余额 | 高 |
| R-005 | 账单列表按日期分组，同一天的数据放在同一张卡片中 | 高 |
| R-006 | 日期卡片顶部左侧显示日期（带今天/昨天/前天标识），右侧显示当日收支汇总 | 高 |
| R-007 | 每条账单显示分类图标、分类名称、备注、金额（红/绿色区分收支）、日期 | 高 |
| R-008 | 列表支持竖向滚动，滚动时标题栏固定在顶部 | 高 |
| R-009 | 列表向上滚动时悬浮新增按钮隐藏，向下滚动时显示 | 中 |
| R-010 | 列表滚动时，账单项的左滑操作自动收起 | 中 |
| R-010.1 | 日期选择弹层从顶部向下弹出，打开时禁用背景页面滚动 | 中 |
| R-010.2 | 日期选择弹层无标题栏，通过背景点击或底部取消按钮关闭 | 低 |

### 2.2 账单操作

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| R-011 | 支持左滑账单项显示编辑和删除按钮（移动端触摸 + 桌面端鼠标拖拽） | 高 |
| R-012 | 删除账单需要二次确认弹窗 | 高 |
| R-013 | 点击编辑按钮打开编辑抽屉，预填充当前账单数据 | 高 |
| R-014 | 保存编辑后留在列表页，不滚动到顶部 | 高 |

### 2.3 新建/编辑抽屉

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| R-015 | 点击悬浮按钮从底部弹出全屏抽屉，URL 带 `?add=true` 参数 | 高 |
| R-016 | 抽屉顶部标题栏高度与列表页一致，中间为支出/收入 Tab，右侧为分类管理入口 | 高 |
| R-017 | 支出/收入 Tab 切换时，下方分类列表自动切换对应类型 | 高 |
| R-018 | 分类以网格形式展示（5列），默认选中第一个分类 | 高 |
| R-019 | 选中的分类高亮显示（支出红色、收入绿色），未选中为白色底色 | 高 |
| R-020 | 分类图标为正方形，图标较大、留白小 | 中 |
| R-021 | 备注输入框在分类下方，铺满剩余空间 | 高 |
| R-022 | 金额输入框在备注下方，支持金额校验（禁止多个小数点、禁止小数点开头、多个0合并等） | 高 |
| R-023 | 金额输入框最小宽度为父元素一半，最大宽度不超过父元素80% | 中 |
| R-024 | 日期选择按钮在金额左侧，点击弹出日历弹窗 | 高 |
| R-025 | 日历弹窗支持今天/昨天/前天快捷选择，支持月份切换和日期点选 | 高 |
| R-026 | 底部左侧为"再记"按钮（连续新建），右侧为"保存"按钮，高度一致 | 高 |
| R-027 | 保存/再记按钮在无分类或金额时禁用 | 高 |
| R-028 | 编辑状态下不显示"再记"按钮 | 中 |
| R-029 | 点击保存后关闭抽屉返回列表，保存编辑后同样关闭抽屉 | 高 |
| R-030 | 手机返回键/浏览器返回按钮在抽屉打开时先关闭抽屉，不退出应用 | 高 |
| R-031 | 从分类管理页返回时，抽屉保持打开状态且无抖动 | 高 |

### 2.4 分类管理

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| R-032 | 独立页面 `/categories`，支出/收入 Tab 切换 | 高 |
| R-033 | 分类以网格展示（5列），与新建页风格一致 | 高 |
| R-034 | 每个分类右上角显示删除按钮（减号图标） | 高 |
| R-035 | 删除分类时若该分类已有账单记录，禁止删除并提示 | 高 |
| R-036 | 支持长按/拖拽排序（桌面端 HTML5 拖拽 + 移动端触摸拖拽） | 高 |
| R-037 | 添加新分类使用弹窗形式，包含名称输入和图标选择（32个图标） | 高 |
| R-038 | 返回按钮回到新建抽屉页面（而非列表页） | 高 |

### 2.5 数据存储

| 需求编号 | 需求描述 | 优先级 |
|---------|---------|-------|
| R-039 | 所有数据使用 localStorage 本地持久化存储 | 高 |
| R-040 | 首次使用自动初始化默认分类和模拟账单数据 | 中 |
| R-041 | 账单数据字段：ID、类型、分类名、图标、金额、备注、日期、时间戳 | 高 |
| R-042 | 分类数据字段：ID、名称、图标、类型 | 高 |

---

## 3. 非功能需求

### 3.1 技术栈

- **前端框架**: React 18 + TypeScript
- **构建工具**: Vite
- **样式方案**: Tailwind CSS
- **状态管理**: Zustand
- **路由**: React Router v6
- **图标**: Lucide React

### 3.2 性能需求

- 页面首屏加载时间 < 2s
- 列表滚动流畅，无卡顿
- 左滑操作响应灵敏，动画流畅

### 3.3 兼容性

- 支持现代浏览器（Chrome、Safari、Edge、Firefox）
- 移动端优先适配，同时支持桌面端操作
- 响应式布局，最大内容宽度 4xl（896px）

### 3.4 用户体验

- 所有操作有即时视觉反馈
- 弹窗/抽屉动画过渡自然
- 关键操作（删除）需二次确认
- 空状态有友好提示

---

## 4. 页面结构

```
/
├── 首页 (Home)
│   ├── 标题栏（年份切换 + 月份筛选）
│   ├── 统计卡片
│   ├── 账单列表（按日期分组）
│   └── 悬浮新增按钮
│
└── /categories
    └── 分类管理页
        ├── 标题栏（Tab + 返回）
        ├── 分类网格（支持拖拽排序 + 删除）
        └── 添加分类弹窗

抽屉组件（AddBillDrawer）- 从首页弹出
├── 标题栏（关闭 + Tab + 设置）
├── 分类选择网格
├── 备注输入
└── 底部操作区（日期 + 金额 + 再记/保存）
    └── 日期选择弹窗
```

---

## 5. 数据结构

### 5.1 账单 (Bill)

```typescript
interface Bill {
  id: string;        // 唯一标识
  type: 'income' | 'expense';  // 类型
  category: string;  // 分类名称
  icon: string;      // 图标名称
  amount: number;    // 金额
  note: string;      // 备注
  date: string;      // 日期 (YYYY-MM-DD)
  timestamp: number; // 创建时间戳
}
```

### 5.2 分类 (Category)

```typescript
interface Category {
  id: string;        // 唯一标识
  name: string;      // 分类名称
  icon: string;      // 图标名称
  type: 'income' | 'expense';  // 类型
}
```

---

## 6. 默认数据

### 6.1 默认支出分类

餐饮、交通、购物、娱乐、居住、医疗、教育、其他

### 6.2 默认收入分类

工资、奖金、投资、兼职、其他

### 6.3 默认图标库

UtensilsCrossed, Car, ShoppingBag, Gamepad2, Home, Heart, GraduationCap, MoreHorizontal, Briefcase, Gift, TrendingUp, Clock, PlusCircle, Plane, Coffee, Book, Music, Smartphone, Wifi, CreditCard, DollarSign, Wallet, PiggyBank, Receipt, ShoppingCart, GiftCard, Beer, Wine, Pizza, IceCream, Cake, Apple

---

## 7. 版本规划

### 7.1 v0.3.0 版本规划（upcoming）

#### 数据导入/导出
- 支持 JSON 格式数据导入导出，方便备份与迁移
- 支持 CSV 格式导出，便于表格软件分析
- 导入时提供数据预览与冲突处理（覆盖/合并/跳过）

#### 账单搜索
- 首页新增搜索入口，支持按备注关键词搜索账单
- 搜索结果按日期倒序排列，高亮匹配内容
- 支持按分类、收支类型、金额范围等高级筛选
- 搜索历史记录，快速复用常用搜索词

#### 自定义时间段统计
- 统计卡片支持切换时间范围：本周/本月/本年/自定义
- 自定义时间段通过日期范围选择器设置
- 统计数据随时间范围实时更新

#### 月度/年度图表统计
- 新增统计页面 `/statistics`，提供图表化数据展示
- 支出分类占比饼图，直观展示消费结构
- 收支趋势折线图，按月或按年展示收支变化
- 分类排行榜，展示支出/收入 Top 分类

#### 预算设置与提醒
- 支持按分类设置月度预算
- 首页显示预算进度条，实时展示已用比例
- 预算超支时红色警示提示
- 支持预算周期重置与历史预算记录查看
